/** Valeurs initiales de référence pour l’identité client (également en migration SQL). */
export {
  DEFAULT_ORGANIZATION_SEED as brandingDefaults,
  DEFAULT_ORGANIZATION_SEED as branding,
} from "../infrastructure/database/types";
